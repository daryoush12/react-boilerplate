import React from 'react'

export default function Default({ children }) {
    return (
        <>
            Hello World
            {children}
            <div>Footer of Main</div>
        </>
    )
}
